services:
  postgres:
    image: postgres:15
    container_name: autorizador-db
    environment:
      POSTGRES_DB: autorizador
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    ports:
      - "5432:5432"